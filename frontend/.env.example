VITE_API_PROXY_TARGET=http://localhost:4000

# Production API base URL (used at build time, leave unset for local dev to use the proxy above)
VITE_API_BASE_URL=https://crm-hrm-application.onrender.com/api
